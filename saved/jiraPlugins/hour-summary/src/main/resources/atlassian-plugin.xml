<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>
    
    <resource type="download" name="hour-summary.png" location="images/hour-summary.png"/>
    
    <gadget name="Hour Summary" key="hour-summary" location="hour-summary.xml"/>
    
    <rest name="Hour Summary Service" key="gadget-rest-resources" path="/hour-summary-service" version="1.0"/>
</atlassian-plugin>